server:
  port: ${PORT:8862}
eureka:
  client:
    register-with-eureka: true
    serviceurl:
      defaultZone: http://localhost:8850/eureka/
message: Congifuracion de Cajero en el servidor levantado
